<div class="carousel ram">
    <p class="lg-flex">RAM</p>
</div>
<div class="flex-row">
    {{#each ram}}
        <div class="flex-product">
        <div class="img">
            <img src="{{this.images.[0]}}" alt="{{this.name}}" height="250" width="250">
        </div>
        <div class="info">
            <p class="logo">{{this.brand}}</p>
            <a href="/ram/{{this.slug}}" class="buy-item">Buy</a>
        </div>
    </div>
    {{/each}}
</div>